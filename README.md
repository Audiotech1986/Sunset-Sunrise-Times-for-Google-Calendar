# Sunset-Sunrise-Times-for-Google-Calendar
a little standalone application to create CSV files so you can add sunrise and sunset times to your google calendar
# Sunrise/Sunset Calendar Generator

A GUI tool to generate Google Calendar CSV files for sunrise and sunset times.

## Requirements
- Python 3.8+
- Packages: `requests`, `pytz`, `tkinter` (standard with Python)

## Usage
1. Run the script:
   ```bash
   python sun_calendar_creator_gui.py
